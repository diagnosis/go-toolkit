// Package middleware provides net/http middleware for authentication,
// CORS, correlation IDs, request logging, and per-IP rate limiting, plus a
// small helper for writing server-sent events.
package middleware
